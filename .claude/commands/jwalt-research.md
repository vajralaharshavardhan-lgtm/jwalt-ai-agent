---
description: Run the J-WALT AI business-development agent (Claude Code as orchestrator, no API keys needed) on a plain-language objective.
---

Follow the procedure in `.claude/skills/jwalt-research/SKILL.md` exactly,
for this objective: $ARGUMENTS

If no objective was given, ask the user for one before doing anything else.
