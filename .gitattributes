# Windows scripts must check out with CRLF line endings regardless of the
# cloning platform's git config -- cmd.exe batch files in particular can
# behave oddly with LF-only line endings.
*.ps1 text eol=crlf
*.bat text eol=crlf
